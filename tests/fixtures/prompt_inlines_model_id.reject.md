# Negative fixture for FR-P2-NOVALUES

A prompt that inlines a routing value the manifests own. The routing fact now has
two owners, and nothing keeps the two equal — the failure this separation exists to
stop. The prompt binds; the data obeys.

## Routing

Safety-critical work routes to gpt-5.6-sol, and the reasoning effort is max.
