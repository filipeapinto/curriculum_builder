"""Curriculum Factory: deterministic, curriculum-neutral execution runtime.

This package is the P01 packaging skeleton only. Production source under
``runtime/`` has not yet been relocated here; that relocation is P03's scope
(spec v8 section 4). This module intentionally re-exports nothing from
``runtime`` and must not import it, so that the wheel and sdist built from
this skeleton contain exactly this file and nothing from the pre-move
production tree.
"""

from __future__ import annotations

__all__: list[str] = []
