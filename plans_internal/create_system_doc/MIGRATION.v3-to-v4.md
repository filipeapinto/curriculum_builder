# Repository migration note: v3 to v4

`create_system_doc.prompt.v2.yaml` targets specification v1, emits a flat source manifest, and uses one direct agent. It is a legacy v1 execution prompt, not a v4 reference implementation. Replace or adapt it before claiming v4 conformance.
