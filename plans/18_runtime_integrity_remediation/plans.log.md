# Runtime Integrity Remediation (arduino_kit_run_v2 Issues 001-007) Planning Log

Append-only record for the planning, QA, test-planning, and implementation-prompt
workflow. Existing entries must not be edited or removed; later corrections are
new entries.

## Objective

Fix the seven defects recorded in issues/001-007 so that runtime/session_bridge.py renders validated unit data as an actual learner lesson, runtime acceptance is fail-closed against the real check inventory in policy/checks.v1.yaml, the visual pipeline produces unit-specific truthful assets and usable evidence cards, L04 states verified model-specific meter guidance and the direct current-mode safety prohibition, Predict-Observe-Explain activities produce real confirmable/disconfirmable evidence, source receipts validate claim entailment and not only byte identity, and the run root records an honest lifecycle/coverage state — then regenerates and re-accepts L01-L04 under the fixed pipeline.

## Entry template

```text
### <UTC timestamp> — <agent/task>
- Action: <work performed>
- Paths touched: <paths, or none>
- Evidence/decision: <verification or decision rationale>
- Issues: <critical/high findings, blockers, or none>
```

## Entries

### 2026-08-03T21:18:13Z — plan_author
- Action: Authored implementation plan v1 covering all seven issues (renderer, visual pipeline, fail-closed acceptance, L04 correction, POE evidence, source-claim entailment, run-level lifecycle state), ordered per issues/README.md's recommended sequence, plus a bounded policy-inventory reconciliation phase and a final regenerate-and-reaccept phase for L01-L04.
- Paths touched: plans/runtime_integrity_remediation/runtime_integrity_remediation.plan.v1.md
- Evidence/decision: Grounded via direct repository reads of runtime/session_bridge.py, runtime/checks.py, policy/checks.v1.yaml, policy/deferred.v1.yaml, schemas/lab.schema.v4.json, curricula/arduino_kit/arduino_kit_curriculum.v5.yaml, tests/gates/fr_p5_unit.py, tests/gates/registry.py, runtime/run_curriculum.py, runtime/controller.py, runtime/checkpoint.py, and outputs/arduino_kit_run_v2/ artifacts, plus an Explore-agent research pass citing exact line ranges for every claim in issues 001-007.
- Issues: None pending critical/high QA.

### 2026-08-03T21:26:15Z — qa_reviewer_round1
- Action: Ran focused Critical/High QA on plan v1.
- Paths touched: plans/runtime_integrity_remediation/qa/plan_qa.v1.md
- Evidence/decision: Verified plan claims against runtime/session_bridge.py, runtime/checks.py, runtime/controller.py, policy/checks.v1.yaml, policy/deferred.v1.yaml, schemas/lab.schema.v4.json, curricula/arduino_kit/*, tests/gates/fr_p5_unit.py, outputs/arduino_kit_run_v2/**.
- Issues: CHANGES REQUIRED - 0 Critical, 2 High. (1) Phase 8's policy/checks.v1.yaml edit risked falsely discharging RT-7 by deleting its note instead of correcting it, and mischaracterized which check ids carry deferred: RT-5. (2) Status/objective misdescribed runtime/controller.py as a 16-line stub when it is a 234-line CurriculumRuntime class that session_bridge.py's prepare() actually calls.

### 2026-08-03T21:26:15Z — plan_author
- Action: Revised plan v1 in place to address both High findings from round-1 QA, without expanding scope.
- Paths touched: plans/runtime_integrity_remediation/runtime_integrity_remediation.plan.v1.md
- Evidence/decision: Finding 1: corrected Phase 8 to only remove deferred: RT-5 from the three PDF-family check ids that actually carry it, and to correct (not delete) TEXT-READABILITY-BAND's RT-7 note in place. Finding 2: replaced the false 16-line-stub claim in Status and objective with an accurate description of CurriculumRuntime.simulate() and its placeholder-only output, matching what prepare() actually calls.
- Issues: None pending; ready for round-2 QA.

### 2026-08-03T21:37:22Z — qa_reviewer_round2
- Action: Ran round-2 focused Critical/High QA on the revised plan v1, mechanically reproducing claims against a scratch copy of the repo where possible (including a direct finalize() re-invocation test).
- Paths touched: plans/runtime_integrity_remediation/qa/plan_qa.v2.md
- Evidence/decision: Round-1's two revisions (controller.py description, RT-7 note handling) were re-verified and hold up. Round 2 found 3 new findings: (1) Critical, mechanically reproduced - re-invoking session_bridge.finalize() against an already-finalized L0N directory raises LogError (ACT already closed) and would also hit FileExistsError on document/ - Section 9's core delivery mechanism does not execute as written. (2) High - Phase 8 claims only 3 check ids carry deferred: RT-5, but grep shows 5 (also PDF-PAGE-COUNT, PDF-PAGE-NONBLANK) plus LAB-SCHEMA-VALID in a different family, all already executing in production yet still marked deferred/unexecuted - Phase 8 as scoped leaves these misrepresented. (3) High - Phase 8's 'Do not touch tests/gates/' contradicts Section 3's required edit to tests/gates/fr_p5_unit.py, and Verification-sequence step 2 depends on that edit having happened.
- Issues: CHANGES REQUIRED - 1 Critical, 2 High (round-2 count of 3 is not lower than round-1 count of 2). Per plan-create skill Step 4 convergence rule, the pipeline halts here rather than proceeding to a third revision/QA round or to Steps 5-7 (execution test plan, prompt, final audit).

### 2026-08-03T22:20:32Z — plan_author
- Action: Revised plan v1 in place to address all 3 round-2 findings, without expanding scope beyond what each finding required.
- Paths touched: plans/runtime_integrity_remediation/runtime_integrity_remediation.plan.v1.md
- Evidence/decision: Finding 1 (Critical): added a reentry_reason parameter to finalize() in Section 3 ('Safe re-entry' bullet) that opens a new logger ACT instead of reusing the closed model_start_id, and clears/recreates document/ before copytree; Section 9's regeneration step now explicitly calls finalize(..., reentry_reason=...) instead of a bare re-invocation; added test (f) to test_acceptance_gate.py covering both the reentry-succeeds and reentry-opt-in-only cases. Finding 2 (High): corrected Phase 8's inventory claim to all 5 pdf: family ids (not 3) plus LAB-SCHEMA-VALID, and expanded Phase 8's scope to remove their stale deferred: RT-5 tags too, each with a verified_by pointer to their existing (pre-plan) call sites. Finding 3 (High): narrowed Phase 8's 'do not touch' boundary to registry.py and gate_families.v1.yaml only, with an explicit carve-out documenting that fr_p5_unit.py is a fixture-gate script Section 3 already requires editing, matching Verification-sequence step 2.
- Issues: None pending; ready for round-3 QA. User has explicitly instructed overriding the skill's default halt-on-non-decrease rule: continue re-QA/revision for up to 4 further rounds beyond round 3 (through round 7) or until a clean (0 Critical, 0 High) verdict, whichever comes first, with every round and revision logged.

### 2026-08-03T22:28:24Z — qa_reviewer_round3
- Action: Ran round-3 focused Critical/High QA, mechanically re-verifying all 3 round-2 fixes in a scratch copy (including a full reentry_reason finalize() run against the real, already-ACCEPTED outputs/arduino_kit_run_v2/L04) and independently scrutinizing sections not yet deeply reviewed.
- Paths touched: plans/runtime_integrity_remediation/qa/plan_qa.v3.md
- Evidence/decision: All 3 round-2 fixes confirmed working as described (reentry succeeds cleanly, opt-in-only behavior confirmed, deferred:RT-5 inventory now matches policy/checks.v1.yaml exactly, tests/gates/ carve-out now explicit). New finding: 1 Critical - Section 2's asset-selection resolver, map renderer, and evidence-card generator all replace code living inside prepare() (session_bridge.py:108-110,146-150), but prepare() cannot be re-invoked against an existing output root (PRECONDITION-OUTPUT-ROOT-EXISTS), and Section 9 regenerates L01-L04 via finalize() only, which never touches output/assets. Mechanically confirmed both halves in a scratch repo. As written, Section 9 would ship L02-L04 with byte-identical stale visuals while falsely reporting issue 003 fixed.
- Issues: CHANGES REQUIRED - 1 Critical, 0 High. Round-3 count (1) is lower than round-2 count (3): converging per skill rule and per explicit user override to continue regardless. Proceeding to fix and round 4.

### 2026-08-03T22:28:24Z — plan_author
- Action: Revised plan v1 in place to address the round-3 Critical finding, without expanding scope beyond what the finding required.
- Paths touched: plans/runtime_integrity_remediation/runtime_integrity_remediation.plan.v1.md
- Evidence/decision: Added a 'Factoring for reuse' bullet to the start of Section 2 requiring the asset-selection resolver, map_kind dispatcher, and evidence-card generator to be written as one function visual_maps.regenerate_assets(unit, curriculum, output), called both from prepare() (fresh units) and directly from Section 9 (regeneration), before finalize()'s reentry call. Updated Section 9's exact-work steps to call regenerate_assets() explicitly before finalize(..., reentry_reason=...), and removed the false 're-rendered visuals (Section 2)' claim from the finalize() bullet since finalize() itself does not generate assets. Added an asset-hash-diff check to Verification-sequence step 3 (addressing round-3's related non-blocking observation that the prior verification sequence could not have caught this) so a future no-op regeneration would be caught mechanically, not just by re-reading the plan.
- Issues: None pending; ready for round-4 QA per user's explicit continue-through-non-convergence instruction.

### 2026-08-03T23:06:44Z — qa_reviewer_round4
- Action: Ran round-4 focused Critical/High QA, mechanically re-verifying round-3's fix and independently walking Section 9's full call sequence plus Sections 4-7 and Acceptance criteria completeness.
- Paths touched: plans/runtime_integrity_remediation/qa/plan_qa.v4.md
- Evidence/decision: Round-3's regenerate_assets() factoring confirmed to solve what it set out to solve. Two new Critical findings, both mechanically reproduced: (1) regenerate_assets() changes asset bytes on disk but nothing updates the corresponding visuals[].provenance.file_hash in the hand-patched workers/lab.json, so finalize()'s unconditional check_receipts() call raises CheckFailure: receipt-hash-mismatch and crashes Section 9's reentrant finalize() for every unit whose assets actually change (L02/L03/L04) - reproduced against the real, already-ACCEPTED outputs/arduino_kit_run_v2/L04. (2) Section 5's instruction to switch L02's domain.build_map to breadboard_build_map is schema-incompatible with Section 2's evidence-card generator, which the plan itself says keeps reading domain.build_map.evidence_card.child_records for L02 after that switch - breadboard_build_map has no evidence_card property and forbids additional properties, confirmed by validating a constructed instance against the real domain.schema.v1.json with jsonschema.
- Issues: CHANGES REQUIRED - 2 Critical, 0 High. Round-4 count (2) is higher than round-3 count (1): does not decrease, which would normally trigger the skill's halt rule. Per explicit user override (continue up to 4 iterations beyond round 3 regardless of convergence trend), proceeding to fix and round 5 rather than halting.

### 2026-08-03T23:06:44Z — plan_author
- Action: Revised plan v1 in place to address both round-4 Critical findings, without expanding scope beyond what each finding required.
- Paths touched: plans/runtime_integrity_remediation/runtime_integrity_remediation.plan.v1.md
- Evidence/decision: Finding 1: added a 'Receipt hashes stay synchronized' bullet to Section 2 specifying regenerate_assets(unit, curriculum, output) takes and returns the unit dict with each regenerated visual's provenance.file_hash (and crop_transform_history) recomputed in place; updated Section 9's sequence to write the returned unit back to workers/lab.json before calling finalize(). Finding 2: added an 'L02 - schema prerequisite' bullet to Section 5 requiring curricula/arduino_kit/domain.schema.v1.json's breadboard_build_map  entry to gain an evidence_card property (matching unpowered_path_map's shape) before the L02 map_kind switch, and added this domain-schema edit to Stop conditions' schema-change bookkeeping alongside the existing lab.schema.v4.json edits.
- Issues: None pending; ready for round-5 QA per user's explicit continue-through-non-convergence instruction (this is iteration 2 of the 4 the user granted beyond round 3).

### 2026-08-03T23:17:28Z — qa_reviewer_round5
- Action: Ran round-5 focused Critical/High QA, mechanically re-verifying all round-4 fixes (including constructing an actual schema-valid L02 breadboard domain instance) and tracing Section 9's full patch/regenerate/finalize sequence end-to-end for L02, L03, and L04 specifically.
- Paths touched: plans/runtime_integrity_remediation/qa/plan_qa.v5.md
- Evidence/decision: Round-4 fixes confirmed to hold up. Three new findings: (1) Critical - the map renderer's connectivity dispatch only defines a wire-pair behavior, but L04's shipped domain.build_map is also map_kind: connectivity with a 4-item socket/dial enumeration, not a wire pair; applying the only specified behavior would falsely render meter sockets as electrically joined, undermining issue 004's safety goal. (2) High - Section 9's patch-target list omitted L03/workers/domain.json even though lab[domain] is byte-identical to it and the issue-006 jumper-rating miscitation lives in that shared data, and Section 3 removes the only check that used to compare the two files, so the divergence would go uncaught. (3) High - the missing-visual-role blocking mechanism was specified with the same 'fails the unit' language as finalize()'s existing hard-abort (raise, no acceptance.json written) paths, contradicting Section 9's Verification requirement that L04's already-certain photography gap produce an actual BLOCKED acceptance.json.
- Issues: CHANGES REQUIRED - 1 Critical, 2 High (count of 3, same as round 2, higher than round 4's 2). Per user's explicit override (continue through round 7 regardless of convergence trend), proceeding to fix and round 6 - this is iteration 3 of the 4 granted beyond round 3.

### 2026-08-03T23:17:28Z — plan_author
- Action: Revised plan v1 in place to address all 3 round-5 findings, without expanding scope beyond what each finding required.
- Paths touched: plans/runtime_integrity_remediation/runtime_integrity_remediation.plan.v1.md
- Evidence/decision: Finding 1: added a 'connectivity discriminator (schema prerequisite)' bullet to Section 2 requiring a new relationship enum (same_wire/enumeration) on domain.schema.v1.json's unpowered_path_map, required whenever map_kind is connectivity; updated the map renderer dispatch to key on (map_kind, relationship) with an explicit enumeration-style rendering for L04; updated the test list accordingly. Finding 2: Section 9's patch-target list now includes L03/workers/domain.json alongside lab.json, with an explanatory note about the shared-data/removed-comparison-check risk. Finding 3: rewrote the 'Blocking on missing roles' bullet to explicitly require the same graceful non-ACCEPTED acceptance.json write used for cross-family bypass (e.g. BLOCKED), not a raised exception, and updated the test list to cover it. Also added the new relationship schema edit to Stop conditions' bookkeeping alongside the other three schema edits.
- Issues: None pending; ready for round-6 QA per user's explicit continue-through-non-convergence instruction (iteration 3 of 4 granted beyond round 3).

### 2026-08-03T23:27:59Z — qa_reviewer_round6
- Action: Ran round-6 focused Critical/High QA, re-verifying round-5 fixes and doing a broad fresh sweep across sections 1, 3, 4, 6, 7, and the Acceptance criteria/Verification sequence, not just the previously-touched Section 9 mechanics.
- Paths touched: plans/runtime_integrity_remediation/qa/plan_qa.v6.md
- Evidence/decision: Round-5 fixes held up. Three new Critical findings, all mechanically confirmed: (1) Section 3's required check set names DOMAIN-SCHEMA-VALID, an id that exists in neither policy/checks.v1.yaml nor curricula/arduino_kit/checks.v1.yaml (grepped both) - contradicts the plan's own not-hardcoded architecture claim. (2) Section 7 claims record_unit_transition is called from finalize(), but Section 3 (which owns every other finalize() edit) never added that call - it only existed as a manual step in Section 9's one-off script, so the fix would not persist for any future unit outside this run. (3) No mechanism or schema field was specified to carry an unresolved visual role's failure from asset-resolution time into finalize()'s BLOCKED decision - lab.schema.v4.json's visual definition cannot represent an unresolved role, and no required check compared declared visual_roles against actual visuals[] entries, so L04's required BLOCKED acceptance.json had no causal path to being produced.
- Issues: CHANGES REQUIRED - 3 Critical, 0 High (count of 3, same as round 5). Per user's explicit override, proceeding to fix and round 7 - this is iteration 4 of the 4 granted beyond round 3, the last one before the pipeline must halt and report to the user regardless of outcome.

### 2026-08-03T23:27:59Z — plan_author
- Action: Revised plan v1 in place to address all 3 round-6 findings, without expanding scope beyond what each finding required.
- Paths touched: plans/runtime_integrity_remediation/runtime_integrity_remediation.plan.v1.md
- Evidence/decision: Finding 1: reworded Section 3's required-check-set bullet to drop the uncatalogued DOMAIN-SCHEMA-VALID id (folding its assertion into the already-catalogued LAB-SCHEMA-VALID) and to add a real DOMAIN-VERIFIER entry to curricula/arduino_kit/checks.v1.yaml pointing at the existing verify_domain.py subprocess call, sourcing the required set from both the engine and curriculum catalogues. Finding 2: added a 'run_state wiring' bullet to Section 3 adding the record_unit_transition call directly inside finalize() after acceptance.json is written; removed the now-redundant manual call from Section 9, keeping only close_run as a manual step. Finding 3: added an 'Unresolved-role signal' bullet to Section 3 introducing a lab.schema.v4.json unresolved_visual_roles[] array populated by regenerate_assets(), a new VISUAL-ROLES-COMPLETE entry in curricula/arduino_kit/checks.v1.yaml, and explicit BLOCKED-before-ACCEPTED_PENDING_REVIEW terminal-state ordering in finalize(); added this as a fourth schema edit to Stop conditions bookkeeping.
- Issues: None pending; ready for round-7 QA - the final iteration granted by the user's explicit continue-through-non-convergence instruction. If round 7 is not clean, the pipeline halts and reports to the user per the granted cap, rather than continuing further.

### 2026-08-03T23:36:59Z — qa_reviewer_round7
- Action: Ran round-7 (final granted iteration) focused Critical/High QA, doing a comprehensive whole-plan pass across all nine Exact-work sections plus Architectural end state, Verification sequence, Acceptance criteria, and Stop conditions, and mechanically re-running the actual tests/gates/run_gates.sh phase-2 gate suite against a scratch copy with the plan's round-6 curriculum-catalogue edits applied.
- Paths touched: plans/runtime_integrity_remediation/qa/plan_qa.v7.md
- Evidence/decision: Round-6 fixes confirmed present and sound as described. Two new Critical findings, both mechanically confirmed: (1) the DOMAIN-VERIFIER and VISUAL-ROLES-COMPLETE entries round 6 added to curricula/arduino_kit/checks.v1.yaml have no matching pattern in that file's own release.advertises table, which flips the real, currently-passing FR-P2-GATEITEMS gate to FAIL when reproduced in a scratch copy - a gate the plan's own Verification sequence step 2 runs, and not covered by the plan's tests/gates/ exclusion since it validates a production catalogue the plan itself edits. (2) The connectivity discriminator's factual premise for L03 is wrong - the plan claims L03's traced_path is a two-item wire pair, but the real shipped data has three items (wire endpoint a, wire endpoint b, expansion board row), and no instruction covers how relationship: same_wire renders the third, non-wire item, reproducing either a false-connectivity or missing-visual-role defect for L03 via the very mechanism round 5 added to prevent it.
- Issues: CHANGES REQUIRED - 2 Critical, 0 High. This is the 4th and final iteration granted by the user's explicit continue-through-non-convergence override (rounds 4,5,6,7 beyond round 3). Per that grant, the pipeline now pauses here rather than launching a round 8 without further user authorization. Round-by-round Critical+High count: R1=2, R2=3, R3=1, R4=2, R5=3, R6=3, R7=2 - never converged to 0.

### 2026-08-07T21:35:24Z — plan_author
- Action: User explicitly authorized a round-8 QA/revision cycle (new grant, separate from the four-iteration cap already exhausted through round 7). Revised plan v1 in place to address both round-7 Critical findings, without expanding scope beyond what each finding required.
- Paths touched: plans/runtime_integrity_remediation/runtime_integrity_remediation.plan.v1.md
- Evidence/decision: Finding 1 (release-table gate break): added a "Release-table advertising (gate prerequisite)" bullet to Section 3, immediately after the DOMAIN-VERIFIER/required-check-set bullet, requiring DOMAIN-VERIFIER and VISUAL-ROLES-COMPLETE be added to curricula/arduino_kit/checks.v1.yaml's static-stage release.advertises list (currently {static: [CAL-*, CUR-*, L01-*], deterministic: [L01-*, LAB-*]}, read directly from the file) in the same edit that adds the two check entries, and added this release-table edit to Stop conditions bookkeeping alongside the four schema edits. Finding 2 (wrong L03 traced_path shape): corrected the connectivity-discriminator bullet's factual claim from "L03's traced_path is a two-item wire pair" to the real three-item shape (["wire endpoint a", "wire endpoint b", "expansion board row"], read directly from outputs/arduino_kit_run_v2/L03/workers/lab.json); updated the map-renderer rewrite's same_wire bullet so it connects only traced_path[0]/[1] and renders any further items (L03's "expansion board row") as their own unconnected labeled point via the enumeration per-item template, rather than assuming exactly two items; updated Section 5's L03 POE bullet so observe/evidence_fields cover both the wire pair and the separately-located expansion row, preserving the existing evidence-card requirement to locate it; added a three-item same_wire fixture case to the test_visual_maps.py test list. Did not touch Section 9's L03 patch instruction (relationship: "same_wire" is still correct for L03; only the renderer's handling of a variable-length traced_path changed) or add any new schema field (no new property was needed; the fix is renderer/instruction-level, keyed on array position).
- Issues: None pending; ready for round-8 QA.

### 2026-08-07T21:43:29Z — qa_reviewer_round8
- Action: Ran round-8 focused Critical/High QA on the revised plan v1, mechanically verifying both round-7 fixes in scratch copies of the repo (gate re-runs, jsonschema validation, direct reads of the shipped L03/L04 worker JSON) and sweeping the round-8 diff for ripple effects.
- Paths touched: plans/runtime_integrity_remediation/qa/plan_qa.v8.md
- Evidence/decision: Round-7 fix 1 verified for what it targeted: adding both new ids plus the new release.advertises entries to a scratch curricula/arduino_kit/checks.v1.yaml keeps FR-P2-GATEITEMS at PASS (baseline 42 staged ids -> 44 staged ids), where round 7 reproduced a FAIL. Round-7 fix 2 verified sound: L03's real build_map.traced_path is exactly the three items the plan now states, lab["domain"] equals workers/domain.json, L04's is the four-item enumeration, the same_wire per-item template is specified in the enumeration bullet below it, and Sections 5 and 9 remain consistent with the new renderer rule - no finding raised against it. Two new findings: (1) Critical - §3's (and §8's) instruction to write verified_by as a pointer to the executing call site is invalid against schemas/checks.schema.v1.json (verified_by pattern ^FR-[A-Z0-9-]+$, oneOf verified_by/deferred), flipping the currently-passing FR-P4-ALL-VALIDATE from PASS to FAIL in a scratch reproduction and producing two advertised-without-owner problems in FR-P4-CHECK-MAPPING's mapping_violations; the round-8 bullet closed the release-surface half of round-7 finding 1 but not this half, and §8 forbids adding a registry gate, so the plan states no legal form. A schema-legal, gate-clean variant was verified. (2) High - the round-8 bullet files both new ids as stage: static, but both assert facts about generated units; every static id in either inventory owns a policy or curriculum source file and every generated-unit check is deterministic or golden, so the entries would enter the inventory claiming a stage that does not run them - a new misrepresentation in the catalogue this plan edits to end misrepresentation, and one FR-P2-GATEITEMS cannot detect.
- Issues: CHANGES REQUIRED - 1 Critical, 1 High. Round-by-round Critical+High count: R1=2, R2=3, R3=1, R4=2, R5=3, R6=3, R7=2, R8=2 - still not converged to 0, though round 8's Critical count fell from 2 to 1 and neither finding is a defect in what round 7 asked to be fixed. Halting here and reporting to the user, per the round-8-only grant.

### 2026-08-07T21:51:40Z — plan_author
- Action: User explicitly authorized one round-9 QA/revision cycle. Revised plan v1 in place to address both round-8 findings, without expanding scope beyond what each finding required, plus the two non-blocking observations round 8 raised while touching the same text.
- Paths touched: plans/runtime_integrity_remediation/runtime_integrity_remediation.plan.v1.md
- Evidence/decision: Finding 1 (Critical, schema-invalid verified_by): §3's DOMAIN-VERIFIER bullet now gives `verified_by: FR-P5-VERIFIER-REQUIRED` (a real registered gate in tests/gates/registry.py) with the verify_domain.py call site (session_bridge.py:244-254) documented in prose instead of in `verified_by`; §3's VISUAL-ROLES-COMPLETE bullet now gives `deferred: RT-5` instead of a file-path pointer, since no registered gate executes that obligation yet. Finding 2 (High, wrong stage): both bullets now specify `stage: deterministic` (matching every other generated-unit check in either inventory) and the Release-table advertising bullet now adds both ids to the deterministic row's `advertises` list instead of static's. Verified in a scratch copy of the real repo with both fixes applied together: 0 jsonschema errors against schemas/checks.schema.v1.json, `tests/gates/fr_p4_policy_schemas.mapping_violations()` reports 0 problems (DOMAIN-VERIFIER VERIFIED HERE, VISUAL-ROLES-COMPLETE MAPPED via RT-5), `bash tests/run_gates.sh 2` reports `FR-P2-GATEITEMS PASS (... 44 staged check ids)`, and `bash tests/run_gates.sh 5` reports `FR-P4-ALL-VALIDATE PASS (12 manifest→schema pairs ...)` - both gates clean together in one scratch state, not checked separately. Non-blocking observation 1: added the release-table edit to "Stop conditions and result" for real (new sentence naming the curricula/arduino_kit/checks.v1.yaml release.advertises edit, alongside the schema edits). Non-blocking observation 2: corrected §3's "the four schema edits already tracked there" (now "the schema edits already tracked there", avoiding a stale count) and corrected the Unresolved-role-signal bullet's "a fourth schema edit" to "a fifth schema edit" (it sits alongside four other named edits, making it the fifth, matching Stop conditions' five: derived[], sourced_claims[], unresolved_visual_roles[], evidence_card, relationship). Did not touch §8's six engine-id entries (policy/checks.v1.yaml) - the user's round-9 authorization scoped this cycle to round 8's two findings on the DOMAIN-VERIFIER/VISUAL-ROLES-COMPLETE entries specifically, and round 8's own "Minimal required remediation" verified fix targeted only these two ids; round-9 QA will independently assess whether §8's analogous wording needs its own finding.
- Issues: None pending; ready for round-9 QA.

### 2026-08-07T21:59:10Z — qa_reviewer_round9
- Action: Ran round-9 focused Critical/High QA on the revised plan v1, independently reproducing both round-8 fixes together in one scratch copy of the repo, and sweeping the round-9 diff (including the two non-blocking-observation fixes) plus §8's unrevised text for ripple effects.
- Paths touched: plans/runtime_integrity_remediation/qa/plan_qa.v9.md
- Evidence/decision: Round-8 fixes confirmed together in one scratch state — adding DOMAIN-VERIFIER (verified_by: FR-P5-VERIFIER-REQUIRED) and VISUAL-ROLES-COMPLETE (deferred: RT-5), both stage: deterministic and advertised under the deterministic row, to curricula/arduino_kit/checks.v1.yaml yields 0 jsonschema errors against schemas/checks.schema.v1.json, 0 mapping_violations() problems (DOMAIN-VERIFIER VERIFIED HERE, VISUAL-ROLES-COMPLETE MAPPED via RT-5), bash tests/run_gates.sh 2 reports FR-P2-GATEITEMS PASS (44 staged check ids), and bash tests/run_gates.sh 5 reports FR-P4-ALL-VALIDATE PASS (12 manifest→schema pairs) — reproduced independently, not just re-trusted. Both non-blocking-observation fixes verified correct and non-contradictory by direct read: Stop conditions now names the release.advertises edit for real, and the four→five schema-edit count is now internally consistent with Stop conditions' own five-item enumeration. New finding: 1 Critical — §8's text is unchanged from round 8 and still instructs removing deferred: RT-5 and adding "a verified_by-style pointer" (a file-path/test-file string) to six ids (LAB-SCHEMA-VALID, PDF-PAGE-COUNT, PDF-PAGE-NONBLANK, PDF-ASSET-RESOLVES, PDF-TEXT-LEGIBLE, PDF-VISUAL-REVIEW) in policy/checks.v1.yaml, the same schema (schemas/checks.schema.v1.json) round 8 found §3's construction illegal against; reproduced mechanically in a scratch copy: 6 jsonschema errors, 6 mapping_violations() advertised-without-owner problems, and FR-P4-ALL-VALIDATE flips PASS→FAIL, mirroring round 8's Finding 1 exactly but for §8. This is the still-open half of round 8's own Finding 1 (whose Minimal Required Remediation named "§3 (and §8)"); plan_author's round-9 revision explicitly scoped itself to §3 only and left §8 untouched. Two non-blocking observations raised: the schema-edit ordinal labels at lines 201/490 don't form a consistent sequence with each other or with Stop conditions' own order (pre-existing, not touched by round 9); §8 already applies the correct deferred-plus-documented-execution pattern to TEXT-READABILITY-BAND's RT-7 note but not to its own six ids, an internal inconsistency whose fix would resolve Finding 1 too. Round 7's and round 8's other carried-forward non-blocking observations remain open, untouched by round 9's scope.
- Issues: CHANGES REQUIRED - 1 Critical, 0 High. Round-by-round Critical+High count: R1=2, R2=3, R3=1, R4=2, R5=3, R6=3, R7=2, R8=2, R9=1 - lower than round 8 but not yet converged to 0; the pipeline is not clean. Reporting to the user per the round-9-only grant.

### 2026-08-08T10:37:40Z — plan_author
- Action: User explicitly authorized one round-10 QA/revision cycle. Revised plan v1 in place to address round-9's one remaining finding, without expanding scope beyond what the finding required.
- Paths touched: plans/runtime_integrity_remediation/runtime_integrity_remediation.plan.v1.md
- Evidence/decision: Finding (Critical, schema-invalid verified_by on six §8 ids): rewrote §8's first bullet so `LAB-SCHEMA-VALID`, `PDF-PAGE-COUNT`, `PDF-PAGE-NONBLANK`, `PDF-ASSET-RESOLVES`, `PDF-TEXT-LEGIBLE`, `PDF-VISUAL-REVIEW` all **keep** `deferred: RT-5` (no longer remove it), since no registered gate in tests/gates/registry.py::GATES executes any of their production call sites and §8 forbids adding one, matching round 9's mechanically-verified Minimal Required Remediation. In place of the removed-deferred/file-path-`verified_by` instruction, §8 now requires adding or correcting each entry's `note` field to state the check already executes at its real call site (session_bridge.py:239/278/287 for the three already-executing ids; the new tests/runtime/test_acceptance_gate.py and session_bridge.py/checks.py production call sites for the three §3 newly wires in) — the same deferred-plus-documented-execution pattern §8 already applies correctly to TEXT-READABILITY-BAND's RT-7 note, left untouched by this edit. Verified together in one scratch copy combining this fix with round 9's already-applied §3 fix (curricula/arduino_kit/checks.v1.yaml's DOMAIN-VERIFIER/VISUAL-ROLES-COMPLETE entries): 0 jsonschema errors against schemas/checks.schema.v1.json for both policy/checks.v1.yaml and curricula/arduino_kit/checks.v1.yaml, 0 tests/gates/fr_p4_policy_schemas.mapping_violations() problems, `bash tests/run_gates.sh 2` reports `FR-P2-GATEITEMS PASS (... 44 staged check ids)`, and `bash tests/run_gates.sh 5` reports `FR-P4-ALL-VALIDATE PASS (12 manifest→schema pairs ...)` — both gates PASS together with all seven affected ids (the two §3 curriculum ids plus the six §8 engine ids) in the same scratch state. Confirmed by direct grep that the `TEXT-READABILITY-BAND`/`RT-7` bullet immediately following §8's rewritten bullet was not touched by this edit and still states RT-7 is unmet.
- Issues: None pending; ready for round-10 QA.

### 2026-08-08T10:42:24Z — qa_reviewer_round10
- Action: Ran round-10 focused Critical/High QA on the revised plan v1, independently reproducing round-10's §8 fix together with round 9's already-verified §3 fix in one scratch copy of the repo, checking the RT-7/TEXT-READABILITY-BAND note for note-deletion-trap regression, and sweeping the plan's Verification sequence, Stop conditions, and Architectural end state for staleness against the six-id note-only rewrite.
- Paths touched: plans/runtime_integrity_remediation/qa/plan_qa.v10.md
- Evidence/decision: §8's first bullet now keeps `deferred: RT-5` on all six ids (`LAB-SCHEMA-VALID`, `PDF-PAGE-COUNT`, `PDF-PAGE-NONBLANK`, `PDF-ASSET-RESOLVES`, `PDF-TEXT-LEGIBLE`, `PDF-VISUAL-REVIEW`) and instead requires adding/correcting each entry's `note` field to document its real call site — the schema-illegal remove-deferred/add-file-path-verified_by instruction is gone. Reproduced from scratch in one fresh scratch copy (`/tmp/qa10_repo`, deleted after use), combining round 9's §3 fix (`DOMAIN-VERIFIER`/`VISUAL-ROLES-COMPLETE` added to `curricula/arduino_kit/checks.v1.yaml`) with round 10's §8 fix (note-only edits to the six `policy/checks.v1.yaml` ids): 0 `jsonschema` errors against `schemas/checks.schema.v1.json` on both files, 0 problems from `tests/gates/fr_p4_policy_schemas.mapping_violations()` called directly against the real combined inventory with `tests/gates/registry.py::GATES` and `policy/deferred.v1.yaml` (`DOMAIN-VERIFIER` VERIFIED HERE; all six §8 ids plus `VISUAL-ROLES-COMPLETE` MAPPED via `RT-5`), `bash tests/run_gates.sh 2` reports `FR-P2-GATEITEMS PASS (... 44 staged check ids)`, `bash tests/run_gates.sh 5` reports `FR-P4-ALL-VALIDATE PASS (12 manifest→schema pairs ...)` — both gates PASS together with all seven affected ids present, matching plan_author's round-10 claim independently. Confirmed by direct read that `TEXT-READABILITY-BAND`'s `RT-7` note bullet immediately below is byte-for-byte unchanged from rounds 8/9 and that the real, unmodified `policy/checks.v1.yaml` entry still carries its original note — the six-id prose neither overwrites nor contradicts RT-7's description, only cites it as a comparison. Swept "Architectural end state" item 6, "Verification sequence", and "Stop conditions and result" for staleness: none makes a claim specific to §8's six ids that the note-only fix contradicts (item 6 requires only a truthful update, satisfied by deferred+note; the fix touches no schema, release table, or check id, so no new Stop-conditions bookkeeping was needed or is missing); §3's required-check-set bullet, unrevised, already documents these three ids as production-executing, consistent with §8's note text. One non-blocking observation: §8's bullet claims all three "§3 newly wires in" ids have `note` fields that "do not yet exist," but `PDF-ASSET-RESOLVES` already has one (`note: corrects B4 ...`) — factually imprecise but does not change what gets written (the exact call site is named per id regardless) or affect schema/gate outcomes, so not raised as a finding.
- Issues: CLEAN - 0 Critical, 0 High. Round-by-round Critical+High count: R1=2, R2=3, R3=1, R4=2, R5=3, R6=3, R7=2, R8=2, R9=1, R10=0 - converged to a clean verdict. Reporting to the user; further pipeline progression (Steps 5-7) requires separate user authorization per the round-10-only grant.

### 2026-08-08T10:56:24Z — plan_author
- Action: Authored execution test plan v1 (Step 5), covering all nine implementation phases plus baseline capture, the external photography prerequisite, full verification-sequence replay, scope-boundary regression, and result/log completeness.
- Paths touched: plans/runtime_integrity_remediation/qa/execution_test.plan.v1.md
- Evidence/decision: Derived directly from the plan's own nine Exact-work sections, Verification sequence, Acceptance criteria, and Stop conditions (runtime_integrity_remediation.plan.v1.md, round-10-clean). Test ids RIR-T00 through RIR-T13, prefix RIR from the slug. Confirmed tests/runtime/ already contains pre-plan tests (test_checks.py, test_controller.py, test_run_curriculum.py, etc.) so RIR-T00's baseline-capture instruction is meaningful. Availability stages section states the photography prerequisite (Section 2) is not a whole-plan blocker per the plan's own text -- only RIR-T10's expected terminal_state values are conditioned on RIR-T01's recorded outcome, and the plan's own Stop-condition (no subject at all croppable) is the one condition that halts the full sequence.
- Issues: None pending; ready for Step 6 (implementation prompt).

### 2026-08-08T10:57:09Z — plan_author
- Action: Authored the Claude Code implementation prompt v1 (Step 6), citing the plan and execution test plan by path rather than re-deriving either.
- Paths touched: plans/runtime_integrity_remediation/prompts/runtime_integrity_remediation.prompt.v1.md
- Evidence/decision: GOAL restates the plan's nine-section scope, explicit out-of-scope boundary (no controller.v1.yaml state machine, no L05-L35, no registry.py/gate_families.v1.yaml edits, deferred.v1.yaml untouched), and the exact schema-legal wording round 8-10 QA converged on for section 8's six ids and section 3's two new curriculum ids, so an implementer cannot silently regress the already-QA'd wording. TEST section enumerates RIR-T00 through RIR-T13 in the same order as qa/execution_test.plan.v1.md, one line each. LOOP restates the plan's own Stop conditions verbatim and names the exact result file and log-append requirement.
- Issues: None pending; ready for Step 7 (independent final audit).

### 2026-08-08T10:58:36Z — final_auditor
- Action: Ran Step 7's independent final audit of the whole package: verified plans.log.md has one entry per stage (plan authoring, all 10 QA rounds, execution-test-plan authoring, prompt authoring) with monotonically increasing timestamps and internally coherent cross-references; sampled Critical/High findings from rounds 2-10 (including round 8's schema-invalid verified_by pointers, round 9's identical §8 defect, and round 10's fix) against the actual text of runtime_integrity_remediation.plan.v1.md as it stands today, not the log's description of it; confirmed qa/execution_test.plan.v1.md's RIR-T00-RIR-T13 ids match prompts/runtime_integrity_remediation.prompt.v1.md's TEST section in identical order with no orphans; confirmed the plan/test-plan/prompt boundary language (L01-L04 only, no controller.v1.yaml state machine, no registry.py/gate_families.v1.yaml edits, policy/deferred.v1.yaml untouched) agrees word-for-word across all three documents; confirmed the photography prerequisite is scoped identically (not a whole-plan blocker, only L02-L04's photographic role) in the plan's §2/§9-step-0, the test plan's Availability stages section, and the prompt's GOAL/LOOP sections. Also ran `python3 .claude/skills/plan-create/scripts/validate_plan_package.py plans/runtime_integrity_remediation`.
- Paths touched: plans/runtime_integrity_remediation/qa/final_audit.v1.md
- Evidence/decision: Direct reads of runtime_integrity_remediation.plan.v1.md (all nine Exact-work sections plus Architectural end state, Verification sequence, Stop conditions), all ten qa/plan_qa.vN.md reports, qa/execution_test.plan.v1.md, and prompts/runtime_integrity_remediation.prompt.v1.md. Validator reported one problem (missing qa/final_audit.v1.md, expected prior to this entry, now resolved) and no other structural defect. Noted as a non-blocking observation (not a Critical/High finding): this session's QA documents are versioned by round number (plan_qa.v1.md-plan_qa.v10.md) rather than by plan version, so the validator's literal v1 lookup resolves to round 1's stale CHANGES-REQUIRED report rather than the authoritative, most-recent qa/plan_qa.v10.md (CLEAN - 0 Critical, 0 High) — a filename/tooling convention gap, not a defect in the plan's content, remediation, or the test/prompt package; plans.log.md and qa/plan_qa.v10.md together make the round-10-clean state unambiguous.
- Issues: PASS - 0 Critical, 0 High remaining. One external prerequisite remains open (photography: official_kit_photo.jpg's croppability for L02/L03/L04 is unverified, L04's is already confirmed unresolvable), consistently scoped as not a whole-plan blocker everywhere it appears. Package is ready to run per prompts/runtime_integrity_remediation.prompt.v1.md; implementation is a separate, later action this audit does not authorize or perform.

### 2026-08-08T13:18:23Z — implementer
- Action: Executed prompts/runtime_integrity_remediation.prompt.v1.md end to end against the round-10-clean plan, running the ordered tests RIR-T00 through RIR-T13 from qa/execution_test.plan.v1.md in strict order. Implemented all nine Exact-work sections: the field-aware renderer (runtime/lesson_render.py), the role- and map-kind-driven visual pipeline (runtime/visual_maps.py), fail-closed acceptance against the real two-file check inventory (runtime/checks.py::required_checks_for, runtime/readability.py, runtime/pdf_inspect.py, a rewritten finalize()), L04's model-agnostic correction, real Predict-Observe-Explain evidence for L02-L04, source-claim entailment (runtime/checks.py::check_claim_entailment plus four regression fixtures), run-level lifecycle state (schemas/run_lifecycle.schema.v1.json, runtime/run_state.py, runtime/workbook.py), the bounded note-only policy/curriculum inventory reconciliation, and the regeneration and re-acceptance of L01-L04.
- Paths touched: runtime/lesson_render.py; runtime/visual_maps.py; runtime/readability.py; runtime/pdf_inspect.py; runtime/run_state.py; runtime/workbook.py; runtime/checks.py; runtime/session_bridge.py; schemas/lab.schema.v4.json; schemas/run_lifecycle.schema.v1.json; policy/checks.v1.yaml; curricula/arduino_kit/checks.v1.yaml; curricula/arduino_kit/domain.schema.v1.json; curricula/arduino_kit/l04_multimeter_evidence.v1.json; curricula/arduino_kit/verified_photo_regions.v1.json; tests/gates/fr_p5_unit.py; tests/__init__.py; tests/runtime/{unit_fixture,test_lesson_render,test_visual_maps,test_acceptance_gate,test_poe_semantics,test_claim_entailment,test_run_state}.py; tests/fixtures/unit_claim_*.json; tests/fixtures/unit_claim_source/; outputs/arduino_kit_run_v2/L01-L04/**; outputs/arduino_kit_run_v2/run_state.json; plans/runtime_integrity_remediation/runtime_integrity_remediation.result.v1.md
- Evidence/decision: All fourteen tests PASS; no Stop condition hit. RIR-T00 baseline recorded a pre-existing blocker the plan did not know about: tests/runtime/ was entirely uncollectable (8 import errors) because tests/runtime/__init__.py shadows the production runtime package under pytest, so Verification-sequence step 1 could not run at all; fixed additively with a one-line tests/__init__.py, which changes no gate outcome. RIR-T01, by direct inspection of official_kit_photo.jpg whole and as four upscaled quadrants: L02 breadboard locatable-and-croppable at pixels (0,640)-(1045,985); L03 wire detail locatable-and-croppable at (0,1225)-(735,1500) showing both wire types; L04 multimeter BLOCKED - no meter, probe or dial appears anywhere in the image, confirming issue 004. Applying section 2's resolver rule to L01's own declared roles additionally found its 'safe disconnected setup photograph' role unresolvable (the image is an inventory flat-lay, not a photograph of a disconnected setup), recorded rather than papered over. All findings are machine-readable in the new curricula/arduino_kit/verified_photo_regions.v1.json. Final terminal states match RIR-T01 exactly: L01 BLOCKED, L02 ACCEPTED_PENDING_REVIEW, L03 ACCEPTED_PENDING_REVIEW, L04 BLOCKED; no unit reaches ACCEPTED because the cross-family judge bypass is present on all four. python3 -m pytest tests/runtime/ reports 166 passed, 14 subtests passed, against a baseline of zero runnable. Gates: FR-P2-GATEITEMS PASS (44 staged ids, exactly the two this plan adds above the baseline's 42), FR-P4-ALL-VALIDATE PASS (12 pairs), 0 jsonschema errors on both checks catalogues, 0 mapping_violations() problems with DOMAIN-VERIFIER VERIFIED HERE and all six section-8 ids plus VISUAL-ROLES-COMPLETE MAPPED via RT-5, phase totals unchanged from baseline in both phases. All six section-8 ids keep deferred: RT-5 with corrected notes naming their real call sites; TEXT-READABILITY-BAND's RT-7 note is corrected, not deleted, and still states RT-7 unmet. tests/gates/registry.py, tests/gates/gate_families.v1.yaml and policy/deferred.v1.yaml are byte-identical to the RIR-T00 copies. All six asset hashes for L02-L04 changed, proving regenerate_assets actually ran, and official_reference.jpg - the whole-kit shot previously substituted into every unit - is gone from L02/L03/L04. Every page of all four rasterized PDFs was inspected by eye at 200 dpi, which caught three real defects fixed before the final pass: missing-glyph tick boxes, overlapping labels in the breadboard cutaway, and a prose/map disagreement in L03's traced-path list. One plan deviation forced by a real gate: content.derived[] and content.unresolved_visual_roles[] sit under content rather than at the top level, because top-level placement flipped the registered FR-P5-UNIT-CONTRACT gate to FAIL on its six-blocks-plus-domain invariant.
- Issues: COMPLETE - RIR-T00 through RIR-T13 all PASS, no test NOT_RUN, no Stop condition triggered. Two checks remain non-PASS and both are honest findings, recorded rather than worked around. (1) VISUAL-ROLES-COMPLETE FAILs on L01 and L04 - the plan's own expected outcome: clearing it needs a human with the physical kit to photograph the multimeter and a disconnected L01 setup, which this plan explicitly forbids acquiring and none was acquired. (2) TEXT-READABILITY-BAND FAILs on L01 at 6.74 against the band [2,6]; this is pre-existing L01 prose the plan does not authorize rewriting, surfaced for the first time because this plan is what made the check execute against real generated text (L02 5.43, L03 5.34, L04 5.90 are all in band). Also recorded rather than hidden: one non-reproducible test failure observed during a batch in which a concurrent pytest was killed by a command timeout; six subsequent runs all passed, the renderer was confirmed deterministic, and the shipped document contains zero brace characters. Nothing was committed or pushed; everything is an inspectable working-tree diff.
