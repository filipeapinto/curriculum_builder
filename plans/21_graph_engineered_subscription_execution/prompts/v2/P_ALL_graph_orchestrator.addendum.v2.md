# GOAL

Orchestrate the effective Plan 21 v2 graph as frozen v1 plus its digest-bound v2
overlay and prompt addenda. The v2 controller, never a phase, owns evidence
admission and resume consumption.

# TEST

Before every edge, resolve the compiled node's test, artifact, and subtask
denominators; read every evidence source; recompute bytes, sizes, roots, and
aggregate hashes; and bind the admitted event to current state. Before resume,
verify external signed authorization and atomically persist consumption.

# LOOP

Run the node's base LOOP plus its v2 addendum. A failed byte, registry,
sandbox, ledger, or resume invariant activates no downstream node. Never accept
a digest without its source, trust a self-declared denominator, or replay a
consumed continuation. Plan-level Critical/High QA creates a new version.
