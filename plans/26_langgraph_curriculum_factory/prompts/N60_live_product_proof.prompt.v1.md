# GOAL

Execute `N60_LIVE_PRODUCT_PROOF` after N50. Prove production behavior with real,
authorized Codex/Gemini/source retrieval/render execution in fresh product roots.

First verify exact CLI/model executable identity, credentials, network,
run-scoped external-data authorization, sandbox isolation, renderer/rasterizer,
and output capacity. If any external prerequisite is absent, perform no partial
transmission and return `NOT_AVAILABLE` with exact evidence. Do not substitute fakes.

When available, run one requested unit plus prerequisite closure and a full
manifest release as required by the spec. Audit actual sources, versions,
visuals, PDFs/pages, independent reviews, checks, checkpoints, evidence logs,
immutable receipts, coverage, terminal guards, and output hashes.

# TEST

1. Authorization and capabilities pass before any curriculum transmission.
2. One-unit run justifies actual `UNIT_ACCEPTED` with complete current denominator.
3. Resume/interruption preserves accepted bytes and makes no duplicate valid calls.
4. Full run justifies actual `COMPLETE` with exact manifest/workbook release audit.
5. Independent recomputation matches terminal, checkpoint, evidence, and artifact hashes.
6. No simulated/fake/prewritten/manual artifact appears in product evidence.

Write `results/N60_LIVE_PRODUCT_PROOF.result.v1.md` with `PASSED`,
`NOT_AVAILABLE`, or `BLOCKED`, plus authorization-safe receipts, commands, exits,
hashes, and any exact prerequisite blocker.

# LOOP

Repair only an implementation defect through the manifest's owning rework edge.
Do not broaden authorization, expose credentials, hand-edit product roots, or
replace live evidence. External prerequisite absence yields `NOT_AVAILABLE`, not
implementation failure and not activation.

