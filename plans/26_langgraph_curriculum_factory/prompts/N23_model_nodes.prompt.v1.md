# GOAL

Implement `N23_MODEL_NODES` after N11/N13. Implement exactly M01–M08 as thin
StateGraph node adapters over the frozen CLI transport. Each builds one exact
authorized projection, invokes one registered job, validates its structured
candidate/findings, and returns typed state updates only.

M01 supports distinct discovery and interpretation packets. M04 handles one
eligible visual brief. M05/M07 review frozen actual-output packets including
every page. M06/M08 receive one named repair boundary. Models cannot admit,
merge, route, accept, resume, or terminate.

# TEST

1. Exactly eight model nodes/jobs and correct Codex/Gemini family split.
2. Each input projection/schema equals the spec and excludes whole state/siblings.
3. Output schemas reject control fields, undeclared artifacts, and broad repair.
4. M05/M07 require exact frozen packet/page denominator and different family.
5. Node retry always traverses D91/D90; no hidden transport/graph retry.
6. Candidate output changes no head until deterministic admission.
7. Fake adapters remain injectable only in test graph builds.

Write `results/N23_MODEL_NODES.result.v1.md` with projection/output tables,
family/authority tests, commands, and hashes.

# LOOP

Patch one projection, schema, adapter, or test. Rerun exact-job, control-field,
and family tests after every change. Stop if any job needs whole state or control authority.

