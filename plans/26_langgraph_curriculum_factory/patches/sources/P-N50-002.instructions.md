Two pre-existing tests in the unchanged suite, `tests/runtime/test_plan26_cli.py`
(`test_plan25_root_refusal_evidence` and `test_preflight_read_only_evidence`),
write live evidence reports as a side effect of simply running, to fixed paths
inside N40's evidence directory:

  plans/26_langgraph_curriculum_factory/results/evidence/N40_CLI_CUTOVER/plan25_resume_refusal.txt
  plans/26_langgraph_curriculum_factory/results/evidence/N40_CLI_CUTOVER/preflight_read_only.txt

Each report embeds a fresh `tempfile.TemporaryDirectory()` path, so the bytes
differ on every run even though the test outcome is identical. Both files are
outside N50's declared write set. N50 must not permanently change them.

Before running `{python} -m pytest -q tests/runtime` (the full suite), use the
Read tool to capture the exact current byte content of both files above.

After the full suite finishes (regardless of pass/fail), compare each file's
content to the captured original. If either changed, use the Write tool to
restore it to exactly the captured original content, so the repository's final
state for both files is byte-identical to what it was before this node ran.
Do this restoration as the last step, after all other N50 work (test file,
result report, N50 evidence) is complete, so it is not clobbered by later steps.

Do not edit `tests/runtime/test_plan26_cli.py` itself to change this behavior —
it is part of the unchanged suite and out of scope for N50. Do not treat this
restoration as a finding to report as a defect in the result report; it is
expected housekeeping caused by running the unchanged suite, not a regression.
Do not restore or touch any file other than these two exact paths.
