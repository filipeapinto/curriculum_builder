The controller only admits a node when its result report contains a bare,
machine-parseable status line matching this exact pattern (multiline regex):

  ^status:\s*(PASSED|NOT_AVAILABLE|BLOCKED)\s*$

That means a line, on its own, starting at column 0 with lowercase `status:`
followed by one of PASSED, NOT_AVAILABLE, or BLOCKED and nothing else on that
line — no trailing parenthetical explanation, no continuation onto following
lines. `results/N31_REPAIR_ACCEPTANCE.result.v1.md` currently opens with:

  status: PASSED (this node's own TEST 1-10 and the P-N31-001 required proof
  all pass; six pre-existing tests in the frozen `test_plan26_unit_graph.py`,
  ...)

which does not match the required pattern, so the controller cannot detect
the reported status and blocks the node even though the work is correct and
complete: N20 and N30 have since been independently corrected (P-N20-001,
P-N30-001) to fix the exact stale assumptions this node's own diagnosis
identified, so the caveat this multi-line status text was trying to capture
no longer describes an open problem.

Fix only the status line itself: replace it with a bare `status: PASSED`
line. Move the explanatory content that was in the parenthetical (or a
short, updated version noting N20/N30 have since corrected the dependent
fixtures) into the body of the report, not the status line — e.g. as a
one-line note directly under "P-N31-001 correction" or wherever it reads
best. Change nothing else in the report's substance, evidence, or hashes.

Then re-run both of this node's required verification commands
(`{python} -m pytest -q tests/runtime/test_plan26_repair_acceptance.py` and
`{python} -m pytest -q tests/runtime/test_plan26_unit_graph.py`) against the
current repository state and confirm both are green now that N30's fixtures
have been corrected — do not assume they still fail; the whole point of
P-N20-001/P-N30-001 was to make them pass. If `test_plan26_unit_graph.py`
still fails for a reason unrelated to what N20/N30 already fixed, report
that honestly rather than adjusting the status line to PASSED anyway.
