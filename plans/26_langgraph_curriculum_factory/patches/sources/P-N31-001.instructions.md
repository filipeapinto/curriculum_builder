Ownership correction from N90's independent audit (finding F1). N90 found that
D16-D23 (the unit repair/acceptance cycle you implement) are fully implemented
and function-level tested, but never registered or wired into the one
production compiled graph (`build_curriculum_factory_graph()` in
`runtime/langgraph_factory/graph.py`). A real run can never reach
`D22_ACCEPT_UNIT`/`UNIT_ACCEPTED` for any unit — only the isolated function
calls your own tests exercise prove anything.

Root cause: your own `repair.py` module docstring says "that registration is
N32's write" — deferring wiring to N32, who deferred it right back (N32's own
prompt says the workbook engine is "not called by `build_curriculum_factory_
graph`... N32 owns wiring this function into whatever the production compile
point becomes once the workbook branch is in scope"). Nobody wired D16-D23.
`graph.py` is now in your write set specifically to close this. Wire it here,
as the node that owns the repair/acceptance node bodies and is best placed to
prove them reachable.

## What "wired" means and how the existing machinery already does most of it

Read `runtime/langgraph_factory/unit_graph.py` in full before changing
anything — do not re-derive its logic from memory. `unit_graph.register_
unit_path(builder, available)` already computes, for every `UNIT_BRANCHES`
source (including D08_VALIDATE_DOMAIN, D09_VALIDATE_CONTENT, D12_VISUAL_
BARRIER_AND_JOIN, D14_INVENTORY_AND_INSPECT_UNIT_PAGES, D91_CLASSIFY_MODEL_
FAILURE, and M05_REVIEW_ACTUAL_UNIT), which of their declared guard
destinations currently have a registered node body (`branch_destinations`,
intersecting `MODEL_BRANCH_DESTINATIONS`/`routing.guard_destinations` against
whatever `available` sequence is passed in) versus which stay deferred
(`deferred_destinations`, checked against the frozen `DEFERRED_EDGES` table).
This means: once D16-D23's node bodies are present in the `bindings`/
`available` sequence that reaches `register_unit_path`, the six DEFERRED_EDGES
rows your work owns —
  (D08_VALIDATE_DOMAIN, domain_repairable) -> D17_CLASSIFY_UNIT_FINDINGS
  (D09_VALIDATE_CONTENT, content_repairable) -> D17_CLASSIFY_UNIT_FINDINGS
  (D12_VISUAL_BARRIER_AND_JOIN, visuals_repairable) -> D17_CLASSIFY_UNIT_FINDINGS
  (D14_INVENTORY_AND_INSPECT_UNIT_PAGES, layout_repairable) -> D17_CLASSIFY_UNIT_FINDINGS
  (D91_CLASSIFY_MODEL_FAILURE, repair) -> D17_CLASSIFY_UNIT_FINDINGS
  (M05_REVIEW_ACTUAL_UNIT, review_returned) -> D16_REDUCE_UNIT_EVIDENCE
get wired automatically the next time `register_skeleton`/`register_unit_path`
runs with that widened set. You do not need to (and must not) edit
`unit_graph.py` itself — it is N30's frozen write, already correct, and
already designed for exactly this widening.

What does not exist yet and is yours to add: (1) getting D16-D23's node
bodies into the bindings passed to `register_skeleton` at the one production
compile point, and (2) the internal topology *within* D16-D23 itself — the
loop spec section 8.2 describes (see the spec's own ASCII diagram, "D16
PASS -> D22 -> D23 -> D05", "D16 FAIL -> D17 -> D18 -> D19 -> [deterministic
producer, or D90 -> M06] -> D20 -> D21 -> earliest invalidated producer/check
in topological order -> D16", "D17/D29 repeat-bound or attempts-exhausted ->
D98"). `UNIT_BRANCHES` in `unit_graph.py` does not declare D16-D23 as sources
at all — that internal loop is not `unit_graph.py`'s concern, it is yours,
the same way `workbook.register_workbook_path` is `workbook.py`'s concern for
D24-D32. Read `runtime/langgraph_factory/workbook.py`'s `register_workbook_
path` and `graph.py`'s `register_workbook_topology` as the worked reference
for this exact pattern (additive registration function, called once from the
one production compile point, wiring a sub-cycle's internal edges over a
builder that already has the skeleton and unit path registered).

## The one constraint that will break other nodes' frozen tests if you miss it

`graph.py`'s `binding_inventory()` function has an explicit docstring warning:
several of N30's own tests in `tests/runtime/test_plan26_unit_graph.py`
independently recompute their expectations directly from `binding_inventory
()`'s *return value*. Do not change what `binding_inventory()` returns. N32
already solved this exact problem for the workbook engine by introducing
`full_binding_inventory()` — a separate, wider function (`binding_inventory()`
plus workbook bindings) used only at the compile point, leaving `binding_
inventory()` itself untouched for N30's tests to keep trusting. Follow the
same pattern for D16-D23: either extend that composition one layer further,
or add your own comparably-named wider function, so `binding_inventory()`'s
return value is provably unchanged (assert this directly in your own test)
while `build_curriculum_factory_graph()` compiles against a set that includes
D16-D23. `tests/runtime/test_plan26_unit_graph.py` is not in your write set
and is a required verification command for this node now specifically so a
regression here is caught before merge, not after.

## Required proof (this is what makes the finding closed, not just "code added")

Your own test file must independently prove, by executing the real compiled
graph — not by re-reading source or trusting N90's fingerprints are gone —
that:
- D16-D23 are members of `build_curriculum_factory_graph(...).get_graph().
  nodes` (an actually compiled graph, the way N90's audit checked it:
  `compiled_topology()`/`compiled.get_graph().nodes`, not a declared table).
- Each of the six DEFERRED_EDGES rows you own is now a real, wired edge in
  that compiled graph, not merely absent from a "still deferred" set.
- A real run through the compiled production graph — using the fake-CLI-
  transport/scripted-response pattern `tests/runtime/test_plan26_adversarial.
  py` already established (see `_ScriptedFakeTransport`, `_HarnessContext`,
  and how N50 drives a real compiled graph end to end with `mn.tp.
  FakeCliTransport`) — reaches `D22_ACCEPT_UNIT`/a real `UNIT_ACCEPTED`
  terminal for at least one unit, exercising both the D16 PASS fast path and
  at least one full D17-D21 repair-and-retest loop back to D16 for a
  deterministic-repair-owned finding. Do not stop at "the edges exist"; prove
  a run actually traverses them.
- No `UnitTopologyError`/`GraphBindingError` is raised by `register_skeleton`
  against the widened bindings (i.e. `validate_bindings`'s placeholder/
  duplicate/uncallable checks and `register_unit_path`'s `N30-EDGE-*` checks
  all pass for D16-D23 exactly as they already do for the existing skeleton).

Do not touch `runtime/langgraph_factory/workbook.py`, `unit_graph.py`, or
D24-D32 in any way — that is N32's write, next. Do not restore or duplicate
anything N32 already owns. Update your own result report to record this
correction: what F1 said, why it was this node's responsibility, and what
changed, with the compiled-graph evidence (node list, edge list, or hashes)
to back it, matching the rigor N90's own audit used.
