`tests/runtime/test_plan26_unit_graph.py` is now also in your write set,
specifically for two residual failures your own generation-2 pass already
diagnosed precisely and correctly declined to route around (per P-N31-002's
own instruction to report honestly rather than force a PASSED status). Fix
exactly these two, nothing else in that file:

1. `test_the_unobservable_boundary_is_excluded_for_a_stated_reason` — its own
   docstring already predicts this exact failure and names you as the owner
   of the rewrite once D16-D23 are wired (its premise — that M05's own update
   is unobservable because the M05->D16 edge is deferred — is now false by
   construction, since D16 has a real body and M05's update is genuinely
   observable). Update this test to assert the correct, current invariant
   instead of the pre-wiring one its docstring already anticipated retiring.
   Do not weaken it into a no-op; assert what is actually true and
   meaningful now (e.g. that the update *is* observable, or whatever the
   test's real underlying concern converts to once its premise inverts —
   read the full test and its docstring before deciding, they already
   explain the intended replacement).

2. `test_a_fresh_episode_executes_the_bootstrap_spine_once_through_langgraph`
   — a real, reproducible LangGraph streaming artifact, not a graph.py/
   repair.py/acceptance.py defect: `compiled.stream(stream_mode="updates")`
   does not yield the update chunk for a step whose *next* step's routing
   raises in the same tick, so when the clean one-unit episode's second
   `D05_SELECT_NEXT_UNIT` call correctly finds the manifest exhausted and
   routes to the still-deferred `D24_PROVE_EXACT_MANIFEST_COVERAGE`, that
   second D05 call's own chunk is swallowed by the same-tick `KeyError`
   before `result["trace"]` ever records it — even though the call is real
   and the state update (`cursor.accepted_ordinal`, cleared
   `selected_unit_id`) is really checkpointed. The test's
   `d05_calls = result["trace"].count("D05_SELECT_NEXT_UNIT")` therefore
   undercounts by exactly one in the exhausted case. Fix the test's own
   assertion to check the real, checkpointed state directly (e.g.
   `state["selected_unit_id"] is None` and `state["cursor"]
   ["accepted_ordinal"] == 1` for the exhausted-after-one-unit case) instead
   of inferring it from a trace-count that this specific stream/exception
   interaction cannot reliably produce. Do not change `compiled.stream()`'s
   call, mode, or anything in `graph.py`/`unit_graph.py` to work around
   this — the trace-collection helper in the test file is what needs to stop
   trusting an undercounted signal, not the compiled graph's own execution.

Do not touch anything else in `test_plan26_unit_graph.py` — the other 91
tests, including the five P-N30-001 already corrected, are exactly right and
must stay exactly as they are. Re-run both of your required verification
commands and confirm both are fully green (93/93 for
`test_plan26_unit_graph.py`, all of `test_plan26_repair_acceptance.py`)
before reporting `status: PASSED` as a bare line. Update your own result
report's "Residual failures" section to reflect that both are now closed,
with the evidence.
