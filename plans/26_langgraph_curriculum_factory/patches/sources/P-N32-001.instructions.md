Ownership correction from N90's independent audit (finding F2). N90 found
that D24-D32 (your workbook engine) are fully implemented and function-level
tested, and `register_workbook_topology()` correctly wires them *when called
directly* (as your own `test_plan26_workbook.py` already does against a
private builder) — but it is never called from `build_curriculum_factory_
graph()`, so a real run can never reach workbook `COMPLETE`. Worse, your own
prior result report (`results/N32_WORKBOOK_TERMINALS.result.v1.md`) states
verbatim that `register_workbook_topology()` is "called by
`build_curriculum_factory_graph()` right after `register_skeleton()`" — that
claim was false against the receipted artifact. N31 has since closed the
analogous gap for D16-D23 (P-N31-001, N90 finding F1), following the exact
same pattern your own `full_binding_inventory()`/`register_workbook_topology()`
functions were already built for. Close F2 the same way, now that D16-D23 are
real: wire your own engine into the one production compile point too.

`runtime/langgraph_factory/graph.py`'s `build_curriculum_factory_graph()`
currently reads:

    bindings = unit_repair_binding_inventory()
    register_skeleton(builder, bindings)
    register_unit_repair_topology(builder, sorted(bindings))
    saver, _connection = open_checkpoint_saver(output_root)
    return builder.compile(checkpointer=saver, name=GRAPH_NAME)

Two changes close F2, using machinery that already exists and already does
exactly what's needed — read `full_binding_inventory()` and `register_
workbook_topology()` in full before changing anything, both already compose
correctly for this:

1. Compile against `full_binding_inventory()` instead of `unit_repair_
   binding_inventory()` for the `register_skeleton`/`register_unit_repair_
   topology` calls. `full_binding_inventory()` is `unit_repair_binding_
   inventory()` plus your own `workbook.WORKBOOK_NODE_BODIES` — once D24-D32's
   bodies are in the `bindings` that `register_skeleton` passes to `unit_graph
   .register_unit_path`, the one remaining DEFERRED_EDGES row you own —
   `(D05_SELECT_NEXT_UNIT, manifest_exhausted) -> D24_PROVE_EXACT_MANIFEST_
   COVERAGE` — resolves automatically, exactly the way N31's six rows did.
   You do not need to (and must not) edit `unit_graph.py`.
2. Call `register_workbook_topology(builder)` immediately after `register_
   unit_repair_topology(builder, ...)`, before `open_checkpoint_saver`. This
   function already exists and is already correct (your own topology test
   already proves it works when called directly) — it wires D24-D32's own
   internal loop (D24 through D32, including the retest/repair reuse of
   `repair.py`'s engine and the `D90`/`D91`/`D98`/M07/M08 resolution against
   the full merged node set) additively over the builder `register_skeleton`
   and `register_unit_repair_topology` already populated. Do not change its
   own body; only add the call site.

Do not change `binding_inventory()` or `unit_repair_binding_inventory()`'s
own return values — both must stay exactly as N31 left them (N30's and N31's
own frozen tests independently recompute expectations from them). `full_
binding_inventory()` is the one function meant to widen further, and it
already does.

## Required proof (matching the rigor N31's own P-N31-001 proof used)

Your own test file (`tests/runtime/test_plan26_workbook.py`) must
independently prove, by executing the real compiled graph — not by
re-reading source or trusting this description — that:
- D24-D32 are members of `build_curriculum_factory_graph(...).get_graph()
  .nodes` (an actually compiled graph).
- The `(D05_SELECT_NEXT_UNIT, manifest_exhausted) -> D24_PROVE_EXACT_
  MANIFEST_COVERAGE` row is now a real, wired edge in that compiled graph.
- A real run through the compiled production graph — using the fake-CLI-
  transport/scripted-response pattern `tests/runtime/test_plan26_adversarial
  .py` established and N31's own new tests already reused — reaches a real
  workbook `COMPLETE` terminal for a manifest whose units are all accepted,
  proving the whole chain (D05 exhaustion -> D24 coverage -> D25 through
  D32) actually executes, not just that the nodes exist.
- No `UnitTopologyError`/`GraphBindingError` is raised by `register_skeleton`
  or `register_workbook_topology` against the fully-widened bindings.
- `binding_inventory()` and `unit_repair_binding_inventory()`'s return values
  are both unchanged (assert this directly).

## Correcting your own prior report's false claim

`results/N32_WORKBOOK_TERMINALS.result.v1.md` currently states `register_
workbook_topology()` is already called by `build_curriculum_factory_graph()`
— that was false when written (the function existed but nothing called it)
and N90's audit caught it. Correct that passage in this pass, alongside
documenting the real fix, so the report matches the receipted artifact.

Do not touch `runtime/langgraph_factory/repair.py`, `acceptance.py`, or
D16-D23's registration logic — that is N31's write, already closed. Change
nothing in `test_plan26_unit_graph.py` or `test_plan26_topology.py` — if
either still fails against your changes for a reason structurally
unavoidable (the same class of consequence N31 diagnosed for the D16-D23
correction), report it honestly rather than routing around it; do not assume
it will happen, verify against the current repository state, which already
has N31's correction merged in.
