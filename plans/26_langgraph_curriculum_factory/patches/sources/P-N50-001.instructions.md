The controller preflight has already proved the resolved interpreter and the
authorized pytest command. Do not probe the interpreter or shell environment.

The only permitted Bash shape is the exact resolved interpreter printed in the
node packet followed by `-m pytest ...`.

Do not run `which`, `ls`, `python --version`, `python -V`, `python -c`, plain
`pytest`, pipes, redirects, command chaining, symlink workarounds, venv creation,
or sandbox-bypass options. Use Read, Glob, and Grep for inspection. Do not ask
for approval for a command outside the allowlist; omit it.

Implement the missing N50 adversarial test matrix before running tests. Then run
the focused N50 file with the exact permitted pytest form. Run the full runtime
suite only after the focused file passes. Treat a permission denial as a harness
finding and report it immediately without repeating variants of the denied call.
