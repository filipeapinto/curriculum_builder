Ownership correction, same chain as P-N20-001: N31 has now (per an approved
immutable patch, P-N31-001) wired D16-D23/M06 into the one production
compiled graph, closing N90's finding F1 — before this, a real run could
never reach `D22_ACCEPT_UNIT`/`UNIT_ACCEPTED` for any unit. Six of your own
tests in `tests/runtime/test_plan26_unit_graph.py` hardcode assumptions from
before that correction and now fail as a direct, structural consequence — not
because `unit_graph.py`'s own logic (`register_unit_path`, `branch_
destinations`, `deferred_destinations`, `DEFERRED_EDGES`, `UNIT_BRANCHES`) is
wrong; N31 did not touch `unit_graph.py` at all, by design (widening the
`available` sequence `register_unit_path` receives is what resolves the six
N31-owned `DEFERRED_EDGES` rows automatically — your module's own machinery
already does this correctly for whatever `available` it's given).

Important ordering note: at the time you run, `runtime/langgraph_factory
/graph.py` still only compiles the narrow, pre-correction `binding_
inventory()` — N31's wiring happens in a separate, isolated attempt and is
merged in only after your fix here is verified and admitted. The six
failures below will not reproduce in your own environment right now. Your
fix must be correct by construction and verified by your existing suite
passing cleanly today — do not hardcode today's specific behavior anywhere,
and do not weaken any assertion's actual guarantee, only what it assumes
about which nodes are currently wired.

- `test_the_compiled_graph_registers_every_declared_unit_branch` computes
  `expected = branch_destinations(source, available)` from `available =
  tuple(sorted(binding_inventory()))` (deliberately narrow, per P-N31-001's
  own instruction not to change what `binding_inventory()` returns), then
  asserts it equals `registered`, read from the real compiled graph's edges.
  Once D08/D09/D12/D14/D91's guards really do route to D17 (and M05's to
  D16) in the real compiled graph, `registered` correctly contains those
  destinations while `expected` — computed from the frozen narrow
  `binding_inventory()` — does not, so the two can no longer be equal by
  construction. Fix this the same way P-N20-001 fixes the analogous topology
  test: derive `available` for this comparison from whatever the real
  compiled graph's own node set is (`set(compiled.get_graph().nodes)`)
  intersected with what `branch_destinations`/`deferred_destinations`
  actually need, rather than from `binding_inventory()`'s always-narrow
  return value — the test's real job is proving `register_unit_path` wired
  exactly the destinations that truly have bodies in whatever the production
  compile point actually used, which is a self-consistency check against the
  compiled graph itself, not a hardcoded external list frozen at whichever
  generation last touched this file.
- `test_a_fresh_episode_executes_the_bootstrap_spine_once_through_langgraph`,
  `test_a_visual_denominator_permutation_produces_an_identical_admitted_head`,
  `test_the_committed_path_stops_at_a_declared_deferred_edge`,
  `test_the_unobservable_boundary_is_excluded_for_a_stated_reason`, and
  `test_this_nodes_renderers_are_a_test_double_and_not_exposed_to_n13s_store_gap`
  all run the shared single-unit `_build_episode_fixture` through
  `_run_episode`, whose scripted M05 review has no blocking findings. Before
  D16 had a real body, that episode necessarily dead-ended at the `M05 ->
  D16` deferred edge — several of these tests hardcode that specific
  frontier (`deferred_frontier == "D16_REDUCE_UNIT_EVIDENCE"`, or assertions
  that only hold because the episode never called `D05_SELECT_NEXT_UNIT` a
  second time). Now that D16 genuinely executes, its denominator legitimately
  passes on the first try for this clean fixture, so the real run proceeds
  through D22/D23, loops back to a second `D05_SELECT_NEXT_UNIT` call, finds
  the one-unit manifest exhausted, and correctly halts on the still-deferred
  `D24_PROVE_EXACT_MANIFEST_COVERAGE` row instead — this is the pipeline
  behaving correctly, exactly what N90's finding said was missing, not a
  defect in this generation's fixture. Do not hardcode the new frontier
  (`D24_...`) as a literal replacement string either, for the same staleness
  reason as above: derive "the deferred edge this clean episode currently
  halts on" from the actual run's own terminal state / `DEFERRED_EDGES`
  lookup at test time, so this suite keeps working once N32 also closes D24.
  If any of these five tests' real purpose (not its specific hardcoded
  frontier) no longer makes sense once D16-D23 execute for real — e.g. a
  test whose entire point was "prove the run stops exactly at the M05->D16
  edge" — say so explicitly in your result report rather than forcing an
  artificial fixture (such as injecting a blocking M05 finding) purely to
  preserve the old stopping point; a real, more-complete pipeline run is the
  correct outcome here, not a regression to route around.

Do not touch `runtime/langgraph_factory/graph.py`'s content, `repair.py`,
`acceptance.py`, or D16-D23's registration logic — none of that is this
node's concern here; this is a test-fixture correction only, in the one file
already in your write set (`tests/runtime/test_plan26_unit_graph.py`). Run
your full existing verification command afterward and confirm it is green.
Update your own result report to record this correction: cite N90's F1 and
P-N31-001 as the root cause chain, and what changed.
