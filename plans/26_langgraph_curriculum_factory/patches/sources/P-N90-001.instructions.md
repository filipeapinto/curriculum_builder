Your own prior attempt found "Finding 1: N40_CLI_CUTOVER receipt is stale" —
`results/evidence/N40_CLI_CUTOVER`'s hash no longer matches its receipt. Read
your own prior attempt's full diagnosis of this (already precise and
correct) before proceeding: the cause is `tests/runtime/test_plan26_cli.py::
PreflightTests::test_preflight_read_only_evidence` (N40's own write set,
part of the required unchanged suite), which writes `results/evidence/
N40_CLI_CUTOVER/preflight_read_only.txt` embedding a `tempfile`-randomized
path on every invocation. This file can never reproduce its receipted bytes,
regardless of any code change — not because N40's CLI cutover is broken, but
because this is a structural, pre-existing non-determinism in one evidence
file of the required, unchanged full suite you must run as part of your own
audit (per your own GOAL: "audit... tests... live proof status", exercised
by actually running the full suite, not just reading source).

This means: the very act of you (or any earlier node, e.g. N50) running the
full suite recreates this exact drift as a side effect, every time,
independent of whether N40's actual CLI cutover work is correct. You do not
have write access to `results/evidence/N40_CLI_CUTOVER` (outside your write
set — restoring it yourself, the way N50's own P-N50-002 patch restores
files within its own write set, is not available to you here), so you
cannot close this the way N50 does. Routing it to N40 as `cli_or_cutover`
rework would not fix anything either: N90 finding F1/F2's own precedent
established that a rework routing exists to correct real implementation
defects, and there is no implementation defect here — the receipted content
was correct at admission time; only this one already-diagnosed,
already-understood non-deterministic evidence byte-stream can never
reproduce itself, by the test's own design, regardless of what N40 does.

Do not route this as a blocking `cli_or_cutover` finding. Instead: verify
that the CURRENT `results/evidence/N40_CLI_CUTOVER` directory's *other*
three files (`import_audit.txt`, `cli_help.txt`, `plan25_resume_refusal.txt`)
still match their receipted content, and that the ONLY drift, if any, is in
`preflight_read_only.txt` specifically, with the same shape of drift your
own prior attempt already documented (a `tempfile`-randomized path substring,
nothing else differing). If — and only if — that is the full extent of the
drift, record it as a non-blocking, informational note (the same way your
own prior attempt's Finding 3 recorded N60's external-credential gap as
informational, not blocking), and evaluate `ACTIVATED`/`IMPLEMENTED_NOT_
ACTIVATED`/`BLOCKED` based on the actual substance of N50's and N60's work,
not on this one file's byte-reproducibility. If you find ANY OTHER drift —
a different file, a different kind of difference, or evidence this reflects
a real behavioral regression rather than a randomized-path artifact — treat
that as a genuine blocking finding and route it normally; do not use this
patch to excuse an unrelated problem.
