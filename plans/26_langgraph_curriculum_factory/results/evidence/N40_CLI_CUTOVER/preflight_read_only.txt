TEST 4: --preflight against an output root that does not yet exist.

exit_code: 20
stdout: {"contract_version": "1", "terminal": {"kind": "SYSTEM_FAILURE", "failure": {"class": "system", "cause": "RepositoryRootError", "message": "no repository data root was supplied. This is required: policy/, schemas/, curricula/, meta_prompt/ and outputs/ belong to the caller, and the installed package's own location is never treated as the repository location. Supply it as an explicit argument, as --engine-root on the CLI, or by setting CURRICULUM_FACTORY_REPOSITORY_ROOT."}}}
stderr: RepositoryRootError: no repository data root was supplied. This is required: policy/, schemas/, curricula/, meta_prompt/ and outputs/ belong to the caller, and the installed package's own location is never treated as the repository location. Supply it as an explicit argument, as --engine-root on the CLI, or by setting CURRICULUM_FACTORY_REPOSITORY_ROOT.
output_root_created: False
