# GOAL

Produce one real authorized full curriculum workbook through the production CLI
and compiled LangGraph, ending in `COMPLETE` with exact manifest and rendered-
page coverage.

N70's accepted unit evidence is a read-only input only. N80 must not resume,
append to, repair, or otherwise write under `outputs/run27/live_unit`, and must
not copy its artifacts into the workbook run.

# TEST

1. Require N70 `PASSED` and current, or emit `NOT_AVAILABLE` when N70 truthfully
   established an unavailable approved subscription driver. Do not reinterpret
   `NOT_AVAILABLE` as product proof.
2. Re-run truthful preflight and freeze the full manifest, ordered unit IDs,
   graph/spec/code/provider digests, resource bounds, and fresh-run identity.
3. Execute the production CLI's full-manifest mode at the fresh output root
   `outputs/run27/live_workbook`, with no manual intermediate curriculum edits.
4. Prove every required unit is accepted exactly once in manifest order and no
   extra, stale, duplicate, failed, or cross-run unit enters assembly.
5. Prove workbook assembly, render, complete page inventory, deterministic checks,
   approved cross-family judgment, bounded workbook-only repair, recomputation,
   and final release all occurred through the production graph.
6. Independently recompute accepted-unit coverage, workbook bytes, page
   denominator, evidence/checkpoint correlation, and sole `COMPLETE` terminal.
7. Prove provider/egress invariants for every live call.
8. If N70 evidence is consulted, prove it was read-only corroboration and that
   no N70 byte was copied, mutated, or admitted into the N80 run.
9. Emit `PASSED` only for a real complete workbook. Approved-driver
   unavailability is `NOT_AVAILABLE`; implementation or product failure is
   `BLOCKED`.

# LOOP

Let declared product repair loops run to their specified bound. Route engine
findings back to their owner and invalidate descendants. Never assemble or edit
the workbook manually, omit a page, narrow the manifest, or substitute a probe
for product execution.
