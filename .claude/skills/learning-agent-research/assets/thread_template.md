# <Thread title — the reviewer archetype this thread investigates>

## Why this thread

<The specific defect observed in the real output that motivated this thread.
Quote the QA report or the document. Name the check that was supposed to catch
it and did not, or say that no check covers it. If a reader cannot tell from
this section which real failure the thread is chasing, the thread is not
grounded yet.>

## Findings

**<One-line claim, bolded.>**
<The evidence, attributed inline to a named source with its identifier
(author/title/arXiv number/vendor). Quote the specific supporting line where a
number or a strong claim is involved.> Implication for this pipeline: <what
this means for the reviewer agent being considered — the sentence that turns a
paper into a design decision.>

**<Next claim.>**
<As above. Three to five findings is typical. If the thread only supports one
finding, say so rather than inflating it.>

## Sources (all fetched and verified to resolve to real, on-topic content)

- <Author(s), "Title," identifier> — <URL>
- <Author(s), "Title," identifier> — <URL>

## Discarded

- <URL> — <why it was rejected: 404, paywall, login wall, unextractable, off
  topic, contradicted the claim, or below the source-quality bar. Include the
  retry attempts so a later scan does not repeat them.>
- <If nothing was discarded in this thread, write "Nothing discarded in this
  thread — every candidate source fetched cleanly and supported its claim.">
